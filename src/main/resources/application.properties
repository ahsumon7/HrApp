# ? PostgreSQL DB Configuration (environment variable based ? recommended for Docker)
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# ? Hibernate JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# ? Optional SQL Logging
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ? Spring Security Default Credentials
spring.security.user.name=sam
spring.security.user.password=sam

#
#
## ? PostgreSQL DB Configuration
#spring.datasource.url=jdbc:postgresql://localhost:5434/employee
#spring.datasource.username=postgres
#spring.datasource.password=isam
#spring.datasource.driver-class-name=org.postgresql.Driver
#
#
## ? Hibernate JPA configuration
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.hibernate.ddl-auto=update
#
## ? Optional SQL logging
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true
#
## ? Spring Security default credentials
#spring.security.user.name=sam
#spring.security.user.password=sam
